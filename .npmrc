# react-scripts@5 declares peerOptional typescript@"^3 || ^4"; this project uses TypeScript 6.
# npm v7+ treats that as a conflict. Legacy peer resolution matches CRA-era behavior.
legacy-peer-deps=true
